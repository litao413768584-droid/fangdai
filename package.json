{
  "name": "fangdai",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev"
  },
  "dependencies": {
    // 你的正常依赖保留不变
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "vite": "^5.4.0",
    // 确保下面这行被删除：
    // "@tailwindcss/oxide": "^4.0.0",  ← 删除这一行
    "typescript": "^5.5.0"
  }
}